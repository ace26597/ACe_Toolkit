# Database (SQLite for development, PostgreSQL for production)
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=mermaid_db
# Use SQLite for local development (no external DB needed)
DATABASE_URL=sqlite+aiosqlite:///./app.db
# For production with PostgreSQL, use:
# DATABASE_URL=postgresql+asyncpg://user:password@db:5432/mermaid_db

# Security
SECRET_KEY=CHANGE_THIS_TO_A_VERY_SECURE_RANDOM_STRING
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=30

# CORS - Configure for your domains
# For development:
ALLOWED_ORIGINS=["http://localhost:3000","http://127.0.0.1:3000"]
# For production, add your domains:
# ALLOWED_ORIGINS=["http://localhost:3000","https://yourdomain.com","https://mermaid-web.vercel.app"]

# Export (Playwright for PDF/image generation)
PLAYWRIGHT_BROWSERS_PATH=/ms-playwright

# AI Configuration - Anthropic Claude API
# Get your API key from https://console.anthropic.com/
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Storage Paths (SSD-backed for data persistence)
# These directories store project data on external SSD
DATA_BASE_DIR=/data
MERMAID_DATA_DIR=/data/mermaid-projects
MEDRESEARCH_DATA_DIR=/data/medresearch-projects
CLAUDE_WORKSPACES_DIR=/data/claude-workspaces
